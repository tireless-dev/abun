{
  "name": "abun",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app/webApp"
  ],
  "scripts": {
    "build": "npm run build -w webApp",
    "start": "npm run start -w webApp"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
